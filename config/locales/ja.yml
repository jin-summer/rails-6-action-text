ja:
  activerecord:
    errors:
      models:
        post:
          attributes:
             title:
                  blank: が空です。
                  too_long: が%{count}文字を超えています。
             content:
                  too_long: が%{max_content_length}文字を超えています。[%{length}/%{max_content_length}]
             base:
                  byte_over: 添付ファイルは%{max_content_mega_byte}MB以下にしてください。[%{byte}/%{max_content_byte}]


    attributes:
      post:
        title: タイトル
        content: コンテンツ


